let val;

val = window.innerWidth;
val = window.outerWidth;

val = window.outerHeight;
val = window.innerHeight;

val = window.scrollX; //가로
val = window.scrollY; //세로

val = window.location; //url에 대한 정보를 가짐

val = window.history; // 사용자가 방문한 기록이 쌓이게 됨

val = window.navigator; // 브라우저의 정보를 가지고 있음


console.log(val)